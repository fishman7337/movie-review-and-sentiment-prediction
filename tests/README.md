# Tests

This folder contains pytest coverage for reusable project logic.

The tests intentionally avoid training deep learning models so CI stays fast and reliable.

Run locally with:

```bash
pytest
```
