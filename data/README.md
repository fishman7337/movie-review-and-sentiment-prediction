# Data

This folder separates local dataset artifacts by lifecycle stage.

- `raw/` stores the original course dataset locally.
- `interim/` stores temporary transformed data.
- `processed/` stores cleaned, model-ready data.

Dataset files are ignored by Git. Commit only documentation and placeholders.
