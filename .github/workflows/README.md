# GitHub Actions Workflows

This folder contains CI workflows for the project.

- `ci.yml` runs linting, tests, security scanning, and project validation.
- `dependency-audit.yml` runs a scheduled Python dependency audit.

Workflows should stay lightweight and avoid training deep learning models.
