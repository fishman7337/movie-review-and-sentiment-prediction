# GitHub Configuration

This folder stores repository automation for GitHub.

- `workflows/` contains CI and security workflow definitions.
- `dependabot.yml` configures dependency and GitHub Actions update checks.

Keep secrets out of this folder. Use GitHub repository secrets for credentials.
