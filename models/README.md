# Models

Use this folder for local trained model artifacts and checkpoints.

Example artifacts:

```text
gru_classification_aug.keras
lstm_regression_no_aug.keras
```

Model files are ignored by Git. Track model metadata and metrics in `reports/` instead.
