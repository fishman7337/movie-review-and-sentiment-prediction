# Metrics

Use this folder for exported metric summaries from experiments.

Recommended formats include JSON, CSV, or Markdown summaries with model name, data split, augmentation setting, and commit SHA.

Generated metric files are ignored by Git by default.
